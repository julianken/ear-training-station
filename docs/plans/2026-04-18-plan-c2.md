# Plan C2: Correctness, Stability, and Documentation

> **For agentic workers:** Use `superpowers:subagent-driven-development` to execute this plan task-by-task. Each task references its GitHub issue for full acceptance criteria.

**Goal:** Fix every correctness gap and pre-launch blocker surfaced by the codebase health analysis, resolve three deferred decision items, wire two spec non-negotiables, and produce documentation that matches the actual post-C1 codebase.

**Decisions locked before execution:**
- #111: Delete `createOrchestrator` dead code (Option A)
- #110: Remove `focus_item_id` field entirely (Option B)
- #109: Include Target/Both replay in C2 as the final feature task (Option A)

**Out of scope for C2:** C2 plan task files, external integrations, new exercise types.

---

## Task 1: Fix AudioContext leak (P0) — GitHub #104

`getAudioContext: () => new AudioContext()` is called on every `startRound()`. Chrome caps concurrent contexts at ~6; past that the worklet never fires, pitch frames are never produced, and every round grades as pitch-fail. SRS data is silently corrupted.

**Files:**
- Modify: `apps/ear-training-station/src/routes/scale-degree/sessions/[id]/+page.svelte` — change factory to session-scoped instance
- Modify: `apps/ear-training-station/src/lib/shell/onboarding/StepWarmupRound.svelte` — same fix for onboarding warmup
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.svelte.ts` — ensure `dispose()` calls `audioContext.close()`

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task1-audiocontext-leak
```

- [ ] **Step 2: Implement** — create one `AudioContext` at session start, pass it to all `startRound()` calls, close in `dispose()`

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #104 in PR body

---

## Task 2: Fix void hydrateShellStores (P0) — GitHub #105

`void hydrateShellStores()` at `+layout.svelte:10` silently swallows IDB failures. Safari private mode → blank dashboard with no error shown.

**Files:**
- Modify: `apps/ear-training-station/src/routes/+layout.svelte` — replace `void` with `.catch()` that logs + sets observable error flag
- Verify: `apps/ear-training-station/src/lib/shell/deps.ts` — confirm `hydrateShellStores` propagates IDB errors

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task2-hydrate-error-handling
```

- [ ] **Step 2: Implement** — catch rejection, surface via `degradationState` or equivalent

- [ ] **Step 3: Test** — add integration test using `fake-indexeddb` configured to throw

- [ ] **Step 4: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 5: PR** — reference #105 in PR body

---

## Task 3: Fix startRound() silent failure (P0) — GitHub #106

`await controller.startRound()` at `ActiveRound.svelte:45-47` has no try/catch. Mic permission denial or AudioContext failure → click "Start round" → nothing happens.

**Files:**
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/ActiveRound.svelte` — wrap in try/catch, show user-visible error
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.test.ts` — add test for rejection path

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task3-startround-error-handling
```

- [ ] **Step 2: Implement** — catch `startRound()` rejection, update `degradationState` with actionable message

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #106 in PR body

---

## Task 4: Dead code cleanup — GitHub #111 + #110

Two related removals: delete the unused `createOrchestrator` implementation and remove the always-null `focus_item_id` field.

**Decision: #111 → Option A (delete). #110 → Option B (remove).**

**Files:**
- Delete: `packages/core/src/session/orchestrator.ts`
- Delete: `packages/core/tests/session/orchestrator-unit.test.ts`
- Delete: `packages/web-platform/tests/session/orchestrator.test.ts`
- Modify: `packages/core/tests/helpers/stub-repos.ts` — remove orchestrator-specific helpers
- Modify: `packages/core/src/types/domain.ts` — remove `focus_item_id` from `Session`
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.svelte.ts` — remove `focus_item_id` from `sessionsRepo.complete()` call
- Modify: any test fixtures with `focus_item_id: null`

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task4-dead-code-cleanup
```

- [ ] **Step 2: Delete orchestrator files** — delete all three files listed above

- [ ] **Step 3: Remove focus_item_id** — from type, controller, test fixtures

- [ ] **Step 4: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 5: PR** — reference #111, #110 in PR body

---

## Task 5: Wire selectNextItem() — GitHub #113

The Leitner + interleaving scheduler (`selection.ts`, 9 unit tests) has zero production imports. Session controller uses naive `dueNow[0]` bypass. Spec non-negotiable: "no blocked practice."

**Files:**
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.svelte.ts` — replace naive selection at line ~305 with `selectNextItem()` call, passing correct `VariabilityHistory`
- Modify: `packages/web-platform/tests/session/` — add anti-repeat integration test

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task5-wire-select-next-item
```

- [ ] **Step 2: Implement** — import `selectNextItem` from `@ear-training/core/scheduler/selection`, pass `lastTimbre`/`lastRegister` from controller state

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #113 in PR body

---

## Task 6: Wire Settings read path — GitHub #114

`auto_advance_on_hit` and `session_length` are persisted to IDB but never read by the session controller. Settings screen is effectively decorative.

**Files:**
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.svelte.ts` — call `settingsRepo.get()` at session init; respect `auto_advance_on_hit` in `_checkCaptureEnd()`
- Verify: `apps/ear-training-station/src/lib/shell/onboarding/` — confirm `session_length` flows to session creation
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.test.ts` — add `auto_advance_on_hit: false` test

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task6-wire-settings-read
```

- [ ] **Step 2: Implement** — read settings at session init; gate auto-advance on the flag

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #114 in PR body

---

## Task 7: Fix pitch trace clock stubs — GitHub #120

`ActiveRound.svelte` passes `windowStartMs: 0` and `getNowMs: () => 0` to `PitchTrace`. All pitch frames have `at_ms ≈ Date.now()` (~1.75T ms), so x-position = billions → clamped to far right. Trace never scrolls.

**Files:**
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/ActiveRound.svelte` — capture `Date.now()` when `PLAYBACK_DONE` fires; pass as `windowStartMs`; pass `() => Date.now()` as `getNowMs`

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task7-pitch-trace-clock
```

- [ ] **Step 2: Implement** — record wall-clock time on `PLAYBACK_DONE` transition, pass to PitchTrace

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #120 in PR body

---

## Task 8: Enable no-floating-promises ESLint rule — GitHub #108

Six PRs across Plan B and C1 introduced floating-promise bugs. This is the single highest-leverage agentic guardrail: catches the pattern at lint time before a PR opens.

**Files:**
- Modify: `eslint.config.mjs` — add `"@typescript-eslint/no-floating-promises": "error"` under TypeScript rules

Known instances that need suppressions or fixes to keep lint green:
- `+layout.svelte:10` — `void hydrateShellStores()` (fixed in Task 2; suppress with comment if Task 8 lands first)
- `session-controller.svelte.ts:345-346` — `.catch(() => {})` on KWS stop (fix in Task 9)

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task8-no-floating-promises-rule
```

- [ ] **Step 2: Enable rule** — add to ESLint config; add minimum suppressions with prose comments to keep lint green

- [ ] **Step 3: Verify** — `pnpm run lint && pnpm run typecheck && pnpm run build`

- [ ] **Step 4: PR** — reference #108 in PR body

---

## Task 9: Floating-promises migration sweep — GitHub #115

With the rule enabled (Task 8), sweep all flagged instances. Fix with proper error handling where failure has user-visible impact; suppress with explanatory comment where genuinely fire-and-forget.

**Blocked by Task 8.**

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task9-floating-promises-sweep
```

- [ ] **Step 2: Sweep** — `pnpm run lint`, fix each flagged instance per policy

- [ ] **Step 3: Verify** — `pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #115 in PR body

---

## Task 10: Integration test: round lifecycle → IDB — GitHub #112

340 unit tests give zero coverage of module composition. Every high-severity bug found in analysis was at the seam between modules. Add a composition test before C3 feature work.

**Files:**
- Create: `packages/web-platform/tests/session/round-lifecycle.test.ts`

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task10-round-lifecycle-test
```

- [ ] **Step 2: Implement** — test: session start → pitch frames injected → CAPTURE_COMPLETE → `attemptsRepo.append()` + `itemsRepo.put()` called correctly; test `$state.snapshot()` boundary (no DataCloneError)

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #112 in PR body

---

## Task 11: Fix DST streak bug — GitHub #116

`StreakChip` computes `tzOffsetMs` at render time. A session recorded near midnight in EDT viewed in EST crosses a day boundary, breaking a valid streak.

**Files:**
- Modify: `packages/core/src/types/domain.ts` — add `tz_offset_ms?: number` to `Session`
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.svelte.ts` — write offset at session creation
- Modify: `packages/core/src/analytics/rollups.ts` — use per-session offset; fall back to 0
- Modify: `apps/ear-training-station/src/lib/shell/StreakChip.svelte` — remove render-time offset
- Modify: `packages/core/tests/analytics/rollups.test.ts` — add DST crossing test

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task11-dst-streak-fix
```

- [ ] **Step 2: Implement** — per-session tz offset, no IDB version bump needed

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #116 in PR body

---

## Task 12: Add +error.svelte — GitHub #121

Unhandled `load()` rejections hit SvelteKit's default white error screen with no navigation out.

**Files:**
- Create: `apps/ear-training-station/src/routes/+error.svelte`

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task12-error-page
```

- [ ] **Step 2: Implement** — dark theme, show `$page.error?.message`, "Back to home" link, accessible `<main>` landmark

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #121 in PR body

---

## Task 13: Wire Target/Both replay — GitHub #109

**Decision: Option A — include in C2.**

FeedbackPanel's Target and Both replay buttons are permanently disabled. `targetAudio = null` is the placeholder; `PlayRoundHandle.targetStartAtAcTime` is the mechanism.

**Files:**
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/session-controller.svelte.ts` — capture target audio buffer when `playRound()` completes; expose as `targetAudio`
- Modify: `apps/ear-training-station/src/lib/exercises/scale-degree/internal/FeedbackPanel.svelte` — enable Target/Both buttons; wire to `controller.targetAudio`

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task13-target-replay
```

- [ ] **Step 2: Implement** — capture + expose target audio, enable disabled buttons

- [ ] **Step 3: Verify** — `pnpm run typecheck && pnpm run test && pnpm run build`

- [ ] **Step 4: PR** — reference #109 in PR body

---

## Task 14: Rewrite CLAUDE.md — GitHub #123 (supersedes #107)

CLAUDE.md describes pre-C1 state: "Plan C1 not yet written", 221 tests (340 actual), wrong head commit. Restructure for agent consumption: commands first, status table, new gotchas (#9–#11).

**Files:**
- Rewrite: `CLAUDE.md` — follow section-by-section plan in GitHub #123 exactly

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task14-rewrite-claude-md
```

- [ ] **Step 2: Implement** — rewrite per issue spec; update test count from `pnpm run test` output at PR time; update HEAD SHA from `git log -1 --format='%H'`

- [ ] **Step 3: Verify** — `pnpm run typecheck` (docs only, must not break)

- [ ] **Step 4: PR** — reference #123, close #107 as superseded

---

## Task 15: Write README.md — GitHub #122

Repository has no README. Human visitors see an empty project page.

**Files:**
- Create: `README.md` — follow section-by-section plan in GitHub #122 exactly (13 sections)

- [ ] **Step 1: Branch**
```bash
git checkout main && git pull
git checkout -b c2/task15-write-readme
```

- [ ] **Step 2: Implement** — follow issue spec; take screenshots via `pnpm run dev` if possible, else placeholder

- [ ] **Step 3: Verify** — `pnpm run typecheck` (docs only, must not break)

- [ ] **Step 4: PR** — reference #122 in PR body

---

## Manual probes (human QA — not subagent work)

These produce no code changes; close them by documenting findings or filing follow-on bugs.

| # | When | What |
|---|------|------|
| #117 | After Tasks 1–3 | Walk a 10+ round session; document any runtime bugs found |
| #118 | Anytime (needs prod build) | Verify offline/PWA flow end-to-end |
| #119 | After Task 2 | Test Safari private mode + Firefox strict privacy |

---

## Execution order summary

```
Wave 1 (P0 blockers):       Task 1 → Task 2 → Task 3
Wave 2 (dead code):         Task 4
Wave 3 (spec violations):   Task 5 → Task 6 → Task 7
Wave 4 (linting):           Task 8 → Task 9
Wave 5 (tests + minor):     Task 10 + Task 11 + Task 12  (independent, can interleave)
Wave 6 (final feature):     Task 13
Wave 7 (docs):              Task 14 + Task 15  (independent)
```
